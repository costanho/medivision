export const environment = {
  production: true,
  // Production defaults - override with real production endpoints during deploy
  authServiceUrl: 'https://api.example.com/auth',
  directServiceUrl: 'https://api.example.com'
};
